<%= labelled_form_for @test_case, :as => :test_case, :url => { :controller => :impasse_test_case, :action => :update, :id => @node.id }, :method => "PUT" do |f| %>
  <%= render :partial => "form", :locals => {:form => f} %>
  <button type="submit" class="ui-button-submit"><%=l(:button_update)%></button>
<% end %>
<% content_for :sidebar %>